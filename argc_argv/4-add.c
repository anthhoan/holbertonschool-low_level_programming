#include <stdio.h>

/**
 * main - program that adds positive numbers
 * @argc: number of arguments
 * @argv: argument vector
 * Return: the added value of numbers, 0 if no numbers passed throgh the program
 * otherwise 1 if symbols that are not digits is passed through
 */

int main(int argc, char *argv[])
{
	int firstNumber = 0;

	if (argc != 3)
	{
		printf("0\n");
		return (0);
	}
	
	if (argc < 48 || argc > 57)
	{
		printf("Error\n");
		return (1);
	}

	while (
	
	return (argc);
}
